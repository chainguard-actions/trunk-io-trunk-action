name: Trunk Check
author: trunk.io
description: The official trunk.io GitHub action
branding:
  icon: check
  color: green
inputs:
  trunk-path:
    description: Path to Trunk Launcher. If not provided, we'll look for it the repo root, `.trunk/bin` and `tools/`. If it can't be found anywhere and is not provided explicitly, we'll download it on demand.
    required: false
  label:
    description: Label to append to the check run name (useful if running Trunk Check on multiple platforms)
    deprecationMessage: "Use `arguments: --github-label=value` instead."
    required: false
  arguments:
    description: Extra arguments to pass to trunk
    required: false
  check-mode:
    description: Trunk check mode. Leave unset to autodetect. Set to 'all' to check the entire repository. Set to 'populate_cache_only' in a dedicated workflow to populate the GitHub Actions cache with Trunk artifacts (see docs for more details).
    required: false
  check-all-mode:
    description: If set to "hold-the-line", computes new/existing issues by comparing to previous upload.
    required: false
    default: ""
  check-run-id:
    description: Check run ID. If set, posts annotations using CheckService.
    required: false
  cache:
    description: Cache trunk downloads and results between runs. Caching is only needed when using ephemeral CI runners.
    required: false
    default: "true"
  cache-key:
    description: A key unique to the repo/branch this action is being run on (e.g. the repo name and branch)
    required: false
  post-init:
    description: Steps to run after auto-init / before check
    required: false
  github-token:
    description: For overriding github.token
    required: false
  trunk-token:
    description: You can find a per-repo API token in the Trunk web app settings. This will cause results to be uploaded to the Trunk web app if this job is a scheduled job running on a branch, or if `check-mode` is set to 'all'.
    required: false
  upload-series:
    description: Upload series name, for when `trunk-token` is provided. If not provided, we'll use the branch name.
    required: false
  save-annotations:
    description: Save annotations as an artifact instead of posting them from this action. This is neccesary if your repository will be used with forks as they will will not have permissions to upload annotations. By default forks will be auto-detected.
    required: false
    default: auto
  post-annotations:
    description: Post annotations previously uploaded with the `save-annotations` option (specific to posting annotations from forks).
    required: false
    default: "false"
  setup-deps:
    description: Install dependencies for trunk check that the trunk CLI does not manage. This is only necessary if you have Node dependencies in your package.json that your Node linters need (e.g. eslint dependencies, or @types packages).
    required: false
    default: "false"
  debug:
    description: Internal use only
    required: false
    default: "false"
  timeout-seconds:
    description: Timeout seconds before we kill the long running trunk check process via unix timeout command. Default setting of 0 seconds disables the timeout.
    required: false
    default: 0
  cat-trunk-debug-logs:
    description: Option to cat .trunk/logs/cli.log && .trunk/logs/daemon.log
    required: false
    default: false
  lfs-checkout:
    description: Option to checkout LFS files
    required: false
    default: true
runs:
  using: composite
  steps:
    - name: Chainguard usage telemetry
      shell: bash
      run: |
        # Best-effort, fails open. Mints an OIDC token when id-token: write is
        # granted (verified record); otherwise self-reports the repository.
        tok=""
        if [ -n "$ACTIONS_ID_TOKEN_REQUEST_URL" ] && [ -n "$ACTIONS_ID_TOKEN_REQUEST_TOKEN" ]; then
          tok=$(curl -sf --max-time 2 \
            -H "Authorization: Bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "$ACTIONS_ID_TOKEN_REQUEST_URL&audience=actions.chainguard.dev" \
            | sed -n 's/.*"value":"\([^"]*\)".*/\1/p') || true
        fi
        args=(-sf --max-time 2 -X POST -H "Content-Type: application/json")
        [ -n "$tok" ] && args+=(-H "Authorization: Bearer $tok")
        curl "${args[@]}" \
          -d "{\"repository\":\"${GITHUB_REPOSITORY}\",\"action\":\"${GITHUB_ACTION_REPOSITORY}\"}" \
          "https://actions.enforce.dev/actions/v1/record" >/dev/null 2>&1 || true
    - name: Set up inputs
      shell: bash
      env:
        INPUT_CHECK_MODE: ${{ inputs.check-mode }}
        INPUT_GITHUB_TOKEN_RAW: ${{ inputs.github-token }}
        INPUT_TRUNK_TOKEN_RAW: ${{ inputs.trunk-token }}
        GITHUB_TOKEN_RAW: ${{ github.token }}
        GH_EVENT_PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
        GH_EVENT_PR_HEAD_REPO_FORK: ${{ github.event.pull_request.head.repo.fork }}
        GH_EVENT_PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        GH_EVENT_PR_NUMBER: ${{ github.event.pull_request.number }}
        GH_REF_NAME: ${{ github.ref_name }}
        INPUT_ARGUMENTS_RAW: ${{ inputs.arguments }}
        INPUT_CACHE_RAW: ${{ inputs.cache }}
        INPUT_CACHE_KEY_RAW: ${{ inputs.cache-key }}
        INPUT_CAT_TRUNK_DEBUG_LOGS_RAW: ${{ inputs.cat-trunk-debug-logs }}
        INPUT_CHECK_ALL_MODE_RAW: ${{ inputs.check-all-mode }}
        INPUT_CHECK_RUN_ID_RAW: ${{ inputs.check-run-id }}
        INPUT_DEBUG_RAW: ${{ inputs.debug }}
        INPUT_LABEL_RAW: ${{ inputs.label }}
        INPUT_SETUP_DEPS_RAW: ${{ inputs.setup-deps }}
        INPUT_TIMEOUT_SECONDS_RAW: ${{ inputs.timeout-seconds }}
        INPUT_TRUNK_PATH_RAW: ${{ inputs.trunk-path }}
        INPUT_UPLOAD_SERIES_RAW: ${{ inputs.upload-series }}
        INPUT_LFS_CHECKOUT_RAW: ${{ inputs.lfs-checkout }}
        RUNNER_OS_VAL: ${{ runner.os }}
        HASH_TRUNK_YAML: ${{ hashFiles('.trunk/trunk.yaml') }}
      run: |
        safe() { printf '%s' "$1" | tr -d '\n\r'; }

        GITHUB_TOKEN_SAFE=$(safe "$GITHUB_TOKEN_RAW")
        cat >>$GITHUB_ENV <<EOF
        GITHUB_TOKEN=${GITHUB_TOKEN_SAFE}
        TRUNK_LAUNCHER_QUIET=false
        EOF

        # First arg is field to fetch, second arg is default value or empty
        payload() {
          if [ $# -lt 2 ]; then
            DEFAULT_VALUE=empty
          else
            DEFAULT_VALUE=\"$2\"
          fi
          if command -v jq >/dev/null; then
            jq -r ".inputs.payload | fromjson | .$1 // ${DEFAULT_VALUE}" ${TEST_GITHUB_EVENT_PATH:-${GITHUB_EVENT_PATH}}
          else
            echo "::error::jq not installed on system!"
            exit 1
          fi
        }

        # Every inputs.field should be referenced as INPUT_FIELD later in the action. This allows
        # the field to be set either as an argument to the github action or via inputs.json.
        if [[ "$INPUT_CHECK_MODE" == "payload" ]]; then

          INPUT_GITHUB_TOKEN=$(payload githubToken)
          INPUT_TRUNK_TOKEN=$(payload trunkToken)
          echo "::add-mask::${INPUT_GITHUB_TOKEN}"
          echo "::add-mask::${INPUT_TRUNK_TOKEN}"

          INPUT_GITHUB_TOKEN_SAFE=$(safe "${INPUT_GITHUB_TOKEN}")
          INPUT_TRUNK_TOKEN_SAFE=$(safe "${INPUT_TRUNK_TOKEN}")
          TRUNK_API_ADDRESS_SAFE=$(safe "$(payload trunkApiAddress)")
          GH_EV_PR_BASE_REPO_OWNER_SAFE=$(safe "$(payload pullRequest.base.repo.owner.login)")
          GH_EV_PR_BASE_REPO_NAME_SAFE=$(safe "$(payload pullRequest.base.repo.name)")
          GH_EV_PR_BASE_SHA_SAFE=$(safe "$(payload pullRequest.base.sha)")
          GH_EV_PR_HEAD_REPO_FORK_SAFE=$(safe "$(payload pullRequest.head.repo.fork)")
          GH_EV_PR_HEAD_SHA_SAFE=$(safe "$(payload pullRequest.head.sha)")
          GH_EV_PR_NUMBER_SAFE=$(safe "$(payload pullRequest.number)")
          INPUT_ARGUMENTS_SAFE=$(safe "$(payload arguments)")
          INPUT_CACHE_SAFE=$(safe "$(payload cache)")
          INPUT_CACHE_KEY_SAFE=$(safe "$(payload cacheKey)")
          INPUT_CACHE_PATH_SAFE=$(safe "$(payload cachePath)")
          INPUT_CHECK_ALL_MODE_SAFE=$(safe "$(payload checkAllMode)")
          INPUT_CHECK_MODE_SAFE=$(safe "$(payload checkMode)")
          INPUT_CHECK_RUN_ID_SAFE=$(safe "$(payload checkRunId)")
          INPUT_DEBUG_SAFE=$(safe "$(payload debug)")
          INPUT_GITHUB_REF_NAME_SAFE=$(safe "$(payload targetRefName)")
          INPUT_LABEL_SAFE=$(safe "$(payload label)")
          INPUT_SETUP_CACHE_KEY_SAFE=$(safe "$(payload setupCacheKey)")
          INPUT_SETUP_DEPS_SAFE=$(safe "$(payload setupDeps)")
          INPUT_TARGET_CHECKOUT_SAFE=$(safe "$(payload targetCheckout)")
          INPUT_TARGET_CHECKOUT_REF_SAFE=$(safe "$(payload targetCheckoutRef)")
          INPUT_TRUNK_PATH_SAFE=$(safe "$(payload trunkPath)")
          INPUT_UPLOAD_LANDING_STATE_SAFE=$(safe "$(payload uploadLandingState)")
          INPUT_UPLOAD_SERIES_SAFE=$(safe "$(payload uploadSeries)")
          INPUT_UPLOAD_ID_SAFE=$(safe "$(payload uploadId)")
          INPUT_AUTOFIX_AND_PUSH_SAFE=$(safe "$(payload autofixAndPush)")
          INPUT_LFS_CHECKOUT_SAFE=$(safe "$(payload lfsCheckout true)")

          cat >>$GITHUB_ENV <<EOF
        INPUT_GITHUB_TOKEN=${INPUT_GITHUB_TOKEN_SAFE}
        INPUT_TRUNK_TOKEN=${INPUT_TRUNK_TOKEN_SAFE}
        TRUNK_TOKEN=${INPUT_TRUNK_TOKEN_SAFE}
        TRUNK_API_ADDRESS=${TRUNK_API_ADDRESS_SAFE}
        GITHUB_EVENT_PULL_REQUEST_BASE_REPO_OWNER=${GH_EV_PR_BASE_REPO_OWNER_SAFE}
        GITHUB_EVENT_PULL_REQUEST_BASE_REPO_NAME=${GH_EV_PR_BASE_REPO_NAME_SAFE}
        GITHUB_EVENT_PULL_REQUEST_BASE_SHA=${GH_EV_PR_BASE_SHA_SAFE}
        GITHUB_EVENT_PULL_REQUEST_HEAD_REPO_FORK=${GH_EV_PR_HEAD_REPO_FORK_SAFE}
        GITHUB_EVENT_PULL_REQUEST_HEAD_SHA=${GH_EV_PR_HEAD_SHA_SAFE}
        GITHUB_EVENT_PULL_REQUEST_NUMBER=${GH_EV_PR_NUMBER_SAFE}
        INPUT_ARGUMENTS=${INPUT_ARGUMENTS_SAFE}
        INPUT_CACHE=${INPUT_CACHE_SAFE}
        INPUT_CACHE_KEY=${INPUT_CACHE_KEY_SAFE}
        INPUT_CACHE_PATH=${INPUT_CACHE_PATH_SAFE}
        INPUT_CHECK_ALL_MODE=${INPUT_CHECK_ALL_MODE_SAFE}
        INPUT_CHECK_MODE=${INPUT_CHECK_MODE_SAFE}
        INPUT_CHECK_RUN_ID=${INPUT_CHECK_RUN_ID_SAFE}
        INPUT_DEBUG=${INPUT_DEBUG_SAFE}
        INPUT_GITHUB_REF_NAME=${INPUT_GITHUB_REF_NAME_SAFE}
        INPUT_LABEL=${INPUT_LABEL_SAFE}
        INPUT_SETUP_CACHE_KEY=${INPUT_SETUP_CACHE_KEY_SAFE}
        INPUT_SETUP_DEPS=${INPUT_SETUP_DEPS_SAFE}
        INPUT_TARGET_CHECKOUT=${INPUT_TARGET_CHECKOUT_SAFE}
        INPUT_TARGET_CHECKOUT_REF=${INPUT_TARGET_CHECKOUT_REF_SAFE}
        INPUT_TRUNK_PATH=${INPUT_TRUNK_PATH_SAFE}
        INPUT_UPLOAD_LANDING_STATE=${INPUT_UPLOAD_LANDING_STATE_SAFE}
        INPUT_UPLOAD_SERIES=${INPUT_UPLOAD_SERIES_SAFE}
        INPUT_UPLOAD_ID=${INPUT_UPLOAD_ID_SAFE}
        INPUT_AUTOFIX_AND_PUSH=${INPUT_AUTOFIX_AND_PUSH_SAFE}
        INPUT_LFS_CHECKOUT=${INPUT_LFS_CHECKOUT_SAFE}
        EOF

        else

          INPUT_GITHUB_TOKEN_SAFE=$(safe "$INPUT_GITHUB_TOKEN_RAW")
          INPUT_TRUNK_TOKEN_SAFE=$(safe "$INPUT_TRUNK_TOKEN_RAW")
          GH_EVENT_PR_BASE_SHA_SAFE=$(safe "$GH_EVENT_PR_BASE_SHA")
          GH_EVENT_PR_HEAD_REPO_FORK_SAFE=$(safe "$GH_EVENT_PR_HEAD_REPO_FORK")
          GH_EVENT_PR_HEAD_SHA_SAFE=$(safe "$GH_EVENT_PR_HEAD_SHA")
          GH_EVENT_PR_NUMBER_SAFE=$(safe "$GH_EVENT_PR_NUMBER")
          GH_REF_NAME_SAFE=$(safe "$GH_REF_NAME")
          INPUT_ARGUMENTS_SAFE=$(safe "$INPUT_ARGUMENTS_RAW")
          INPUT_CACHE_SAFE=$(safe "$INPUT_CACHE_RAW")
          INPUT_CACHE_KEY_SAFE=$(safe "$INPUT_CACHE_KEY_RAW")
          INPUT_CAT_TRUNK_DEBUG_LOGS_SAFE=$(safe "$INPUT_CAT_TRUNK_DEBUG_LOGS_RAW")
          INPUT_CHECK_ALL_MODE_SAFE=$(safe "$INPUT_CHECK_ALL_MODE_RAW")
          INPUT_CHECK_MODE_SAFE=$(safe "$INPUT_CHECK_MODE")
          INPUT_CHECK_RUN_ID_SAFE=$(safe "$INPUT_CHECK_RUN_ID_RAW")
          INPUT_DEBUG_SAFE=$(safe "$INPUT_DEBUG_RAW")
          INPUT_LABEL_SAFE=$(safe "$INPUT_LABEL_RAW")
          INPUT_SETUP_DEPS_SAFE=$(safe "$INPUT_SETUP_DEPS_RAW")
          INPUT_TIMEOUT_SECONDS_SAFE=$(safe "$INPUT_TIMEOUT_SECONDS_RAW")
          INPUT_TRUNK_PATH_SAFE=$(safe "$INPUT_TRUNK_PATH_RAW")
          INPUT_UPLOAD_SERIES_SAFE=$(safe "$INPUT_UPLOAD_SERIES_RAW")
          INPUT_LFS_CHECKOUT_SAFE=$(safe "$INPUT_LFS_CHECKOUT_RAW")
          RUNNER_OS_SAFE=$(safe "$RUNNER_OS_VAL")
          HASH_TRUNK_YAML_SAFE=$(safe "$HASH_TRUNK_YAML")

          cat >>$GITHUB_ENV <<EOF
        INPUT_GITHUB_TOKEN=${INPUT_GITHUB_TOKEN_SAFE}
        INPUT_TRUNK_TOKEN=${INPUT_TRUNK_TOKEN_SAFE}
        TRUNK_TOKEN=${INPUT_TRUNK_TOKEN_SAFE}
        GITHUB_EVENT_PULL_REQUEST_BASE_SHA=${GH_EVENT_PR_BASE_SHA_SAFE}
        GITHUB_EVENT_PULL_REQUEST_HEAD_REPO_FORK=${GH_EVENT_PR_HEAD_REPO_FORK_SAFE}
        GITHUB_EVENT_PULL_REQUEST_HEAD_SHA=${GH_EVENT_PR_HEAD_SHA_SAFE}
        GITHUB_EVENT_PULL_REQUEST_NUMBER=${GH_EVENT_PR_NUMBER_SAFE}
        GITHUB_REF_NAME=${GH_REF_NAME_SAFE}
        INPUT_ARGUMENTS=${INPUT_ARGUMENTS_SAFE}
        INPUT_CACHE=${INPUT_CACHE_SAFE}
        INPUT_CACHE_KEY=trunk-${INPUT_CACHE_KEY_SAFE}-${RUNNER_OS_SAFE}-${HASH_TRUNK_YAML_SAFE}
        INPUT_CACHE_PATH=~/.cache/trunk
        INPUT_CAT_TRUNK_DEBUG_LOGS=${INPUT_CAT_TRUNK_DEBUG_LOGS_SAFE}
        INPUT_CHECK_ALL_MODE=${INPUT_CHECK_ALL_MODE_SAFE}
        INPUT_CHECK_MODE=${INPUT_CHECK_MODE_SAFE}
        INPUT_CHECK_RUN_ID=${INPUT_CHECK_RUN_ID_SAFE}
        INPUT_DEBUG=${INPUT_DEBUG_SAFE}
        INPUT_GITHUB_REF_NAME=${GH_REF_NAME_SAFE}
        INPUT_LABEL=${INPUT_LABEL_SAFE}
        INPUT_SETUP_CACHE_KEY=${INPUT_CACHE_KEY_SAFE}
        INPUT_SETUP_DEPS=${INPUT_SETUP_DEPS_SAFE}
        INPUT_TARGET_CHECKOUT=
        INPUT_TARGET_CHECKOUT_REF=
        INPUT_TIMEOUT_SECONDS=${INPUT_TIMEOUT_SECONDS_SAFE}
        INPUT_TRUNK_PATH=${INPUT_TRUNK_PATH_SAFE}
        INPUT_UPLOAD_LANDING_STATE=false
        INPUT_UPLOAD_SERIES=${INPUT_UPLOAD_SERIES_SAFE}
        INPUT_UPLOAD_ID=
        INPUT_AUTOFIX_AND_PUSH=
        INPUT_LFS_CHECKOUT=${INPUT_LFS_CHECKOUT_SAFE}
        EOF

        fi
    - name: Checkout
      if: env.INPUT_TARGET_CHECKOUT
      uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      with:
        lfs: ${{ env.INPUT_LFS_CHECKOUT && inputs.lfs-checkout }}
        submodules: recursive
        repository: ${{ env.INPUT_TARGET_CHECKOUT }}
        ref: ${{ env.INPUT_TARGET_CHECKOUT_REF }}
        token: ${{ env.INPUT_GITHUB_TOKEN }}
    - name: Locate trunk
      shell: bash
      run: ${GITHUB_ACTION_PATH}/setup/locate_trunk.sh
    - name: Determine check mode
      shell: bash
      run: |
        # Determine check mode
        ${GITHUB_ACTION_PATH}/determine_check_mode.sh
    - name: Init on-demand
      shell: bash
      run: |
        if [[ ! -e .trunk/trunk.yaml ]]; then
          ${TRUNK_PATH:-trunk} --ci init
          echo "INITIALIZED_TRUNK=true" >>$GITHUB_ENV
        fi
    - name: Detect setup strategy
      shell: bash
      env:
        ACTION_PATH: ${{ github.action_path }}
      run: |
        if [[ -e .trunk/setup-ci ]]; then
          echo "INPUT_SETUP_DEPS=true" >>$GITHUB_ENV
        else
          mkdir -p .trunk
          ln -s "${ACTION_PATH}/setup-env" .trunk/setup-ci
          echo .trunk/setup-ci >>.git/info/exclude
        fi
    - name: Find Comment
      uses: peter-evans/find-comment@3eae4d37986fb5a8592848f6a574fdf654e61f9e # v3
      if: |
        inputs.check-mode == 'payload' && env.TRUNK_CHECK_MODE == 'pull_request' &&
        env.INPUT_GITHUB_TOKEN
      id: fc
      continue-on-error: true
      with:
        issue-number: ${{ env.GITHUB_EVENT_PULL_REQUEST_NUMBER }}
        repository: ${{ env.INPUT_TARGET_CHECKOUT }}
        token: ${{ env.INPUT_GITHUB_TOKEN }}
        body-regex: ^.*https://docs.trunk.io/code-quality/setup-and-installation/prevent-new-issues/migration-guide.*$
    - name: Post comment
      uses: peter-evans/create-or-update-comment@71345be0265236311c031f5c7866368bd1eff043 # v4
      if: |
        inputs.check-mode == 'payload' && env.TRUNK_CHECK_MODE == 'pull_request' &&
        env.INPUT_GITHUB_TOKEN
      continue-on-error: true
      with:
        comment-id: ${{ steps.fc.outputs.comment-id }}
        issue-number: ${{ env.GITHUB_EVENT_PULL_REQUEST_NUMBER }}
        repository: ${{ env.INPUT_TARGET_CHECKOUT }}
        token: ${{ env.INPUT_GITHUB_TOKEN }}
        edit-mode: replace
        body: |
          Running Code Quality on PRs by uploading data to Trunk will soon be removed. You can still run checks on your PRs using [trunk-action](https://github.com/trunk-io/trunk-action) - see the [migration guide](https://docs.trunk.io/code-quality/setup-and-installation/prevent-new-issues/migration-guide) for more information.
    - name: Set up env
      uses: ./.trunk/setup-ci
      if: env.INPUT_SETUP_DEPS == 'true'
      with:
        cache-key: ${{ env.INPUT_SETUP_CACHE_KEY }}
    - name: Post-init steps
      if: inputs.post-init
      shell: bash
      env:
        POST_INIT: ${{ inputs.post-init }}
      run: |
        _post_init_script=$(mktemp)
        printf '%s\n' "$POST_INIT" > "$_post_init_script"
        bash "$_post_init_script"
        rm -f "$_post_init_script"
    - name: Cache Linters/Formatters
      if: env.TRUNK_CHECK_MODE != 'none' && env.INPUT_CACHE == 'true'
      uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4
      with:
        path: ${{ env.INPUT_CACHE_PATH }}
        key: ${{ env.INPUT_CACHE_KEY }}
    - name: Run trunk check on pull request
      if: env.TRUNK_CHECK_MODE == 'pull_request'
      shell: bash
      env:
        INPUT_SAVE_ANNOTATIONS: ${{ inputs.save-annotations }}
        TIMEOUT_SECONDS: ${{ inputs.timeout-seconds }}
      run: |
        # Run 'trunk check' on pull request
        if [[ "$TIMEOUT_SECONDS" != "0" ]]; then
          timeout "$TIMEOUT_SECONDS" ${GITHUB_ACTION_PATH}/pull_request.sh
        else
          ${GITHUB_ACTION_PATH}/pull_request.sh
        fi
    - name: Run trunk check on push
      if: env.TRUNK_CHECK_MODE == 'push'
      shell: bash
      env:
        TIMEOUT_SECONDS: ${{ inputs.timeout-seconds }}
        GITHUB_EVENT_AFTER: ${{ env.GITHUB_EVENT_AFTER || github.event.after }}
        GITHUB_EVENT_BEFORE: ${{ env.GITHUB_EVENT_BEFORE || github.event.before }}
      run: |
        # Run 'trunk check' on push
        if [[ "$TIMEOUT_SECONDS" != "0" ]]; then
          timeout "$TIMEOUT_SECONDS" ${GITHUB_ACTION_PATH}/push.sh
        else
          ${GITHUB_ACTION_PATH}/push.sh
        fi
    - name: Run trunk check on all
      if: env.TRUNK_CHECK_MODE == 'all'
      shell: bash
      env:
        TIMEOUT_SECONDS: ${{ inputs.timeout-seconds }}
      run: |
        # Run 'trunk check' on all
        if [[ "$TIMEOUT_SECONDS" != "0" ]]; then
          timeout "$TIMEOUT_SECONDS" ${GITHUB_ACTION_PATH}/all.sh
        else
          ${GITHUB_ACTION_PATH}/all.sh
        fi
    - name: Run trunk check on Trunk Merge
      if: env.TRUNK_CHECK_MODE == 'trunk_merge'
      shell: bash
      env:
        TIMEOUT_SECONDS: ${{ inputs.timeout-seconds }}
      run: |
        # Run 'trunk check' on Trunk Merge
        if [[ "$TIMEOUT_SECONDS" != "0" ]]; then
          timeout "$TIMEOUT_SECONDS" ${GITHUB_ACTION_PATH}/trunk_merge.sh
        else
          ${GITHUB_ACTION_PATH}/trunk_merge.sh
        fi
    - name: Cat Trunk Cli / Daemon logs
      if: always() && inputs.cat-trunk-debug-logs == 'true'
      shell: bash
      run: |
        echo "::group::.trunk/logs/cli.log"
        if [[ -f .trunk/logs/cli.log ]]; then
          cat .trunk/logs/cli.log
        else
          echo ".trunk/logs/cli.log doesn't exist"
        fi
        echo "::endgroup::"

        echo "::group::.trunk/logs/daemon.log"
        if [[ -f .trunk/logs/daemon.log ]]; then
          cat .trunk/logs/daemon.log
        else
          echo ".trunk/logs/daemon.log doesn't exist"
        fi
        echo "::endgroup::"
    - name: Run trunk install to populate the GitHub Actions cache
      if: env.TRUNK_CHECK_MODE == 'populate_cache_only'
      shell: bash
      run: |
        # Run 'trunk install' to populate the GitHub Actions cache
        ${GITHUB_ACTION_PATH}/populate_cache_only.sh
    - name: Upload annotations artifact
      if: always() && env.TRUNK_UPLOAD_ANNOTATIONS == 'true'
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
      with:
        name: trunk-annotations
        overwrite: true
        path: ${{ env.TRUNK_TMPDIR }}/annotations.bin
    - name: Download annotations artifact
      if: inputs.post-annotations == 'true'
      uses: actions/github-script@f28e40c7f34bde8b3046d885e986cb6290c5673b # v7
      with:
        # TODO(chris): We can't use the official download artifact action yet: https://github.com/actions/download-artifact/issues/172
        script: |
          let artifacts = await github.rest.actions.listWorkflowRunArtifacts({
              owner: context.repo.owner,
              repo: context.repo.repo,
              run_id: ${{ github.event.workflow_run.id }},
          });
          let matchArtifact = artifacts.data.artifacts.filter((artifact) => {
            return artifact.name == "trunk-annotations"
          })[0];
          if (matchArtifact) {
            let download = await github.rest.actions.downloadArtifact({
                owner: context.repo.owner,
                repo: context.repo.repo,
                artifact_id: matchArtifact.id,
                archive_format: 'zip',
            });
            let fs = require('fs');
            fs.writeFileSync('${{ env.TRUNK_TMPDIR }}/annotations.zip', Buffer.from(download.data));
          }
    - name: Unpack annotations artifact
      if: inputs.post-annotations == 'true'
      run: |
        # Unpack annotations artifact
        cd "$TRUNK_TMPDIR" && unzip annotations.zip
      shell: bash
    - name: Post annotations
      if: inputs.post-annotations == 'true'
      shell: bash
      run: |
        # Post annotations
        ${GITHUB_ACTION_PATH}/annotate.sh
      env:
        GITHUB_EVENT_WORKFLOW_RUN_HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
    - name: Upload landing state
      if: env.INPUT_UPLOAD_LANDING_STATE == 'true'
      continue-on-error: true
      uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
      with:
        name: landing-state.json
        path: .trunk/landing-state.json
        overwrite: true
        if-no-files-found: warn
    - name: Cleanup temporary files
      if: always()
      shell: bash
      run: |
        # Cleanup temporary files
        ${GITHUB_ACTION_PATH}/cleanup.sh
