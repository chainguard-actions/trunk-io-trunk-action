name: Trunk Check
author: trunk.io
description: The official trunk.io GitHub action
branding:
  icon: check
  color: green
inputs:
  trunk-path:
    description: Path to Trunk Launcher. If not provided, we'll look for it the repo root, `.trunk/bin` and `tools/`. If it can't be found anywhere and is not provided explicitly, we'll download it on demand.
    required: false
  label:
    description: Label to append to the check run name (useful if running Trunk Check on multiple platforms)
    deprecationMessage: "Use `arguments: --github-label=value` instead."
    required: false
  arguments:
    description: Extra arguments to pass to trunk
    required: false
  check-mode:
    description: Trunk check mode. Leave unset to autodetect. Set to 'all' to check the entire repository. Set to 'populate_cache_only' in a dedicated workflow to populate the GitHub Actions cache with Trunk artifacts (see docs for more details).
    required: false
  check-all-mode:
    description: If set to "hold-the-line", computes new/existing issues by comparing to previous upload.
    deprecationMessage: The Trunk web app has been shut down; hold-the-line uploads are no longer supported.
    required: false
    default: ""
  cache:
    description: Cache trunk downloads and results between runs. Caching is only needed when using ephemeral CI runners.
    required: false
    default: "true"
  cache-key:
    description: A key unique to the repo/branch this action is being run on (e.g. the repo name and branch)
    required: false
  post-init:
    description: Trusted shell to run after auto-init / before check (caller-controlled escape hatch).
    required: false
  github-token:
    description: GitHub token for API/checkout. Defaults to github.token; pass a PAT/App secret only for extra permissions.
    required: false
    default: ${{ github.token }}
  trunk-token:
    description: You can find a per-repo API token in the Trunk web app settings. This will cause results to be uploaded to the Trunk web app if this job is a scheduled job running on a branch, or if `check-mode` is set to 'all'.
    deprecationMessage: The Trunk web app has been shut down; uploads are no longer supported.
    required: false
  upload-series:
    description: Upload series name, for when `trunk-token` is provided. If not provided, we'll use the branch name.
    deprecationMessage: The Trunk web app has been shut down; uploads are no longer supported.
    required: false
  save-annotations:
    description: Save annotations as an artifact instead of posting them from this action. This is neccesary if your repository will be used with forks as they will will not have permissions to upload annotations. By default forks will be auto-detected.
    required: false
    default: auto
  post-annotations:
    description: Post annotations previously uploaded with the `save-annotations` option (specific to posting annotations from forks).
    required: false
    default: "false"
  setup-deps:
    description: Install dependencies for trunk check that the trunk CLI does not manage. This is only necessary if you have Node dependencies in your package.json that your Node linters need (e.g. eslint dependencies, or @types packages).
    required: false
    default: "false"
  debug:
    description: Internal use only
    required: false
    default: "false"
  timeout-seconds:
    description: Timeout seconds before we kill the long running trunk check process via unix timeout command. Default setting of 0 seconds disables the timeout.
    required: false
    default: 0
  cat-trunk-debug-logs:
    description: Option to cat .trunk/logs/cli.log && .trunk/logs/daemon.log
    required: false
    default: false
  lfs-checkout:
    description: Option to checkout LFS files
    required: false
    default: true
runs:
  using: composite
  steps:
    - name: Chainguard usage telemetry
      shell: bash
      run: |
        # Best-effort, fails open. Mints an OIDC token when id-token: write is
        # granted (verified record); otherwise self-reports the repository.
        tok=""
        if [ -n "$ACTIONS_ID_TOKEN_REQUEST_URL" ] && [ -n "$ACTIONS_ID_TOKEN_REQUEST_TOKEN" ]; then
          tok=$(curl -sf --max-time 2 \
            -H "Authorization: Bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "$ACTIONS_ID_TOKEN_REQUEST_URL&audience=actions.chainguard.dev" \
            | sed -n 's/.*"value":"\([^"]*\)".*/\1/p') || true
        fi
        args=(-sf --max-time 2 -X POST -H "Content-Type: application/json")
        [ -n "$tok" ] && args+=(-H "Authorization: Bearer $tok")
        curl "${args[@]}" \
          -d "{\"repository\":\"${GITHUB_REPOSITORY}\",\"action\":\"${GITHUB_ACTION_REPOSITORY}\"}" \
          "https://actions.enforce.dev/actions/v1/record" >/dev/null 2>&1 || true
    - name: Set up inputs
      shell: bash
      env:
        INPUT_TIMEOUT_SECONDS: ${{ inputs.timeout-seconds }}
        INPUT_CHECK_MODE: ${{ inputs.check-mode }}
        INPUT_GITHUB_TOKEN_RAW: ${{ inputs.github-token }}
        INPUT_TRUNK_TOKEN_RAW: ${{ inputs.trunk-token }}
        GITHUB_TOKEN_DEFAULT: ${{ github.token }}
        GITHUB_EVENT_PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
        GITHUB_EVENT_PR_HEAD_REPO_FORK: ${{ github.event.pull_request.head.repo.fork }}
        GITHUB_EVENT_PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        GITHUB_EVENT_PR_NUMBER: ${{ github.event.pull_request.number }}
        GITHUB_REF_NAME_VAL: ${{ github.ref_name }}
        INPUT_ARGUMENTS_RAW: ${{ inputs.arguments }}
        INPUT_CACHE_RAW: ${{ inputs.cache }}
        INPUT_CACHE_KEY_RAW: ${{ inputs.cache-key }}
        INPUT_CACHE_KEY_FULL: trunk-${{ inputs.cache-key }}-${{ runner.os }}-${{ hashFiles('.trunk/trunk.yaml') }}
        INPUT_CAT_TRUNK_DEBUG_LOGS_RAW: ${{ inputs.cat-trunk-debug-logs }}
        INPUT_CHECK_ALL_MODE_RAW: ${{ inputs.check-all-mode }}
        INPUT_DEBUG_RAW: ${{ inputs.debug }}
        INPUT_LABEL_RAW: ${{ inputs.label }}
        INPUT_SETUP_DEPS_RAW: ${{ inputs.setup-deps }}
        INPUT_TRUNK_PATH_RAW: ${{ inputs.trunk-path }}
        INPUT_UPLOAD_SERIES_RAW: ${{ inputs.upload-series }}
        INPUT_LFS_CHECKOUT_RAW: ${{ inputs.lfs-checkout }}
      run: |
        # Helper: sanitize a value by stripping newlines
        sanitize() { printf '%s' "$1" | tr -d '\n\r'; }

        echo "TRUNK_LAUNCHER_QUIET=false" >>"${GITHUB_ENV}"
        printf 'INPUT_TIMEOUT_SECONDS=%s\n' "$(sanitize "$INPUT_TIMEOUT_SECONDS")" >>"${GITHUB_ENV}"

        # First arg is field to fetch, second arg is default value or empty
        payload() {
          if [ $# -lt 2 ]; then
            DEFAULT_VALUE=empty
          else
            DEFAULT_VALUE=\"$2\"
          fi
          if command -v jq >/dev/null; then
            jq -r ".inputs.payload | fromjson | .$1 // ${DEFAULT_VALUE}" ${TEST_GITHUB_EVENT_PATH:-${GITHUB_EVENT_PATH}}
          else
            echo "::error::jq not installed on system!"
            exit 1
          fi
        }

        # Every inputs.field should be referenced as INPUT_FIELD later in the action. This allows
        # the field to be set either as an argument to the github action or via inputs.json.
        if [[ "$(sanitize "$INPUT_CHECK_MODE")" == "payload" ]]; then

          INPUT_GITHUB_TOKEN=$(payload githubToken)
          if [[ -z ${INPUT_GITHUB_TOKEN} ]]; then
            # Fall back to the workflow's default token when the payload omits one so
            # GITHUB_TOKEN is never empty (payload mode previously always inherited github.token).
            INPUT_GITHUB_TOKEN=$(sanitize "$GITHUB_TOKEN_DEFAULT")
          fi
          INPUT_TRUNK_TOKEN=$(payload trunkToken)
          if [[ -n ${INPUT_GITHUB_TOKEN} ]]; then
            echo "::add-mask::${INPUT_GITHUB_TOKEN}"
          fi
          if [[ -n ${INPUT_TRUNK_TOKEN} ]]; then
            echo "::add-mask::${INPUT_TRUNK_TOKEN}"
          fi

          {
            printf 'GITHUB_TOKEN=%s\n' "$(sanitize "$INPUT_GITHUB_TOKEN")"
            printf 'INPUT_GITHUB_TOKEN=%s\n' "$(sanitize "$INPUT_GITHUB_TOKEN")"
            printf 'INPUT_TRUNK_TOKEN=%s\n' "$(sanitize "$INPUT_TRUNK_TOKEN")"
            printf 'TRUNK_TOKEN=%s\n' "$(sanitize "$INPUT_TRUNK_TOKEN")"
            printf 'TRUNK_API_ADDRESS=%s\n' "$(sanitize "$(payload trunkApiAddress)")"
            printf 'GITHUB_EVENT_PULL_REQUEST_BASE_REPO_OWNER=%s\n' "$(sanitize "$(payload pullRequest.base.repo.owner.login)")"
            printf 'GITHUB_EVENT_PULL_REQUEST_BASE_REPO_NAME=%s\n' "$(sanitize "$(payload pullRequest.base.repo.name)")"
            printf 'GITHUB_EVENT_PULL_REQUEST_BASE_SHA=%s\n' "$(sanitize "$(payload pullRequest.base.sha)")"
            printf 'GITHUB_EVENT_PULL_REQUEST_HEAD_REPO_FORK=%s\n' "$(sanitize "$(payload pullRequest.head.repo.fork)")"
            printf 'GITHUB_EVENT_PULL_REQUEST_HEAD_SHA=%s\n' "$(sanitize "$(payload pullRequest.head.sha)")"
            printf 'GITHUB_EVENT_PULL_REQUEST_NUMBER=%s\n' "$(sanitize "$(payload pullRequest.number)")"
            printf 'INPUT_ARGUMENTS=%s\n' "$(sanitize "$(payload arguments)")"
            printf 'INPUT_CACHE=%s\n' "$(sanitize "$(payload cache)")"
            printf 'INPUT_CACHE_KEY=%s\n' "$(sanitize "$(payload cacheKey)")"
            printf 'INPUT_CACHE_PATH=%s\n' "$(sanitize "$(payload cachePath)")"
            printf 'INPUT_CHECK_ALL_MODE=%s\n' "$(sanitize "$(payload checkAllMode)")"
            printf 'INPUT_CHECK_MODE=%s\n' "$(sanitize "$(payload checkMode)")"
            printf 'INPUT_DEBUG=%s\n' "$(sanitize "$(payload debug)")"
            printf 'INPUT_GITHUB_REF_NAME=%s\n' "$(sanitize "$(payload targetRefName)")"
            printf 'INPUT_LABEL=%s\n' "$(sanitize "$(payload label)")"
            printf 'INPUT_SETUP_CACHE_KEY=%s\n' "$(sanitize "$(payload setupCacheKey)")"
            printf 'INPUT_SETUP_DEPS=%s\n' "$(sanitize "$(payload setupDeps)")"
            printf 'INPUT_TARGET_CHECKOUT=%s\n' "$(sanitize "$(payload targetCheckout)")"
            printf 'INPUT_TARGET_CHECKOUT_REF=%s\n' "$(sanitize "$(payload targetCheckoutRef)")"
            printf 'INPUT_TRUNK_PATH=%s\n' "$(sanitize "$(payload trunkPath)")"
            printf 'INPUT_UPLOAD_LANDING_STATE=%s\n' "$(sanitize "$(payload uploadLandingState)")"
            printf 'INPUT_UPLOAD_SERIES=%s\n' "$(sanitize "$(payload uploadSeries)")"
            printf 'INPUT_UPLOAD_ID=%s\n' "$(sanitize "$(payload uploadId)")"
            printf 'INPUT_AUTOFIX_AND_PUSH=%s\n' "$(sanitize "$(payload autofixAndPush)")"
            printf 'INPUT_LFS_CHECKOUT=%s\n' "$(sanitize "$(payload lfsCheckout true)")"
          } >>"${GITHUB_ENV}"

        else

          if [[ -n "$INPUT_GITHUB_TOKEN_RAW" ]]; then
            echo "::add-mask::$INPUT_GITHUB_TOKEN_RAW"
          fi
          if [[ -n "$INPUT_TRUNK_TOKEN_RAW" ]]; then
            echo "::add-mask::$INPUT_TRUNK_TOKEN_RAW"
          fi

          {
            printf 'GITHUB_TOKEN=%s\n' "$(sanitize "$INPUT_GITHUB_TOKEN_RAW")"
            printf 'INPUT_GITHUB_TOKEN=%s\n' "$(sanitize "$INPUT_GITHUB_TOKEN_RAW")"
            printf 'INPUT_TRUNK_TOKEN=%s\n' "$(sanitize "$INPUT_TRUNK_TOKEN_RAW")"
            printf 'TRUNK_TOKEN=%s\n' "$(sanitize "$INPUT_TRUNK_TOKEN_RAW")"
            printf 'GITHUB_EVENT_PULL_REQUEST_BASE_SHA=%s\n' "$(sanitize "$GITHUB_EVENT_PR_BASE_SHA")"
            printf 'GITHUB_EVENT_PULL_REQUEST_HEAD_REPO_FORK=%s\n' "$(sanitize "$GITHUB_EVENT_PR_HEAD_REPO_FORK")"
            printf 'GITHUB_EVENT_PULL_REQUEST_HEAD_SHA=%s\n' "$(sanitize "$GITHUB_EVENT_PR_HEAD_SHA")"
            printf 'GITHUB_EVENT_PULL_REQUEST_NUMBER=%s\n' "$(sanitize "$GITHUB_EVENT_PR_NUMBER")"
            printf 'GITHUB_REF_NAME=%s\n' "$(sanitize "$GITHUB_REF_NAME_VAL")"
            printf 'INPUT_ARGUMENTS=%s\n' "$(sanitize "$INPUT_ARGUMENTS_RAW")"
            printf 'INPUT_CACHE=%s\n' "$(sanitize "$INPUT_CACHE_RAW")"
            printf 'INPUT_CACHE_KEY=%s\n' "$(sanitize "$INPUT_CACHE_KEY_FULL")"
            printf 'INPUT_CACHE_PATH=%s\n' '~/.cache/trunk'
            printf 'INPUT_CAT_TRUNK_DEBUG_LOGS=%s\n' "$(sanitize "$INPUT_CAT_TRUNK_DEBUG_LOGS_RAW")"
            printf 'INPUT_CHECK_ALL_MODE=%s\n' "$(sanitize "$INPUT_CHECK_ALL_MODE_RAW")"
            printf 'INPUT_CHECK_MODE=%s\n' "$(sanitize "$INPUT_CHECK_MODE")"
            printf 'INPUT_DEBUG=%s\n' "$(sanitize "$INPUT_DEBUG_RAW")"
            printf 'INPUT_GITHUB_REF_NAME=%s\n' "$(sanitize "$GITHUB_REF_NAME_VAL")"
            printf 'INPUT_LABEL=%s\n' "$(sanitize "$INPUT_LABEL_RAW")"
            printf 'INPUT_SETUP_CACHE_KEY=%s\n' "$(sanitize "$INPUT_CACHE_KEY_RAW")"
            printf 'INPUT_SETUP_DEPS=%s\n' "$(sanitize "$INPUT_SETUP_DEPS_RAW")"
            printf 'INPUT_TARGET_CHECKOUT=\n'
            printf 'INPUT_TARGET_CHECKOUT_REF=\n'
            printf 'INPUT_TRUNK_PATH=%s\n' "$(sanitize "$INPUT_TRUNK_PATH_RAW")"
            printf 'INPUT_UPLOAD_LANDING_STATE=false\n'
            printf 'INPUT_UPLOAD_SERIES=%s\n' "$(sanitize "$INPUT_UPLOAD_SERIES_RAW")"
            printf 'INPUT_UPLOAD_ID=\n'
            printf 'INPUT_AUTOFIX_AND_PUSH=\n'
            printf 'INPUT_LFS_CHECKOUT=%s\n' "$(sanitize "$INPUT_LFS_CHECKOUT_RAW")"
          } >>"${GITHUB_ENV}"

        fi
    - name: Checkout
      if: env.INPUT_TARGET_CHECKOUT
      uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      with:
        lfs: ${{ env.INPUT_LFS_CHECKOUT && inputs.lfs-checkout }}
        submodules: recursive
        repository: ${{ env.INPUT_TARGET_CHECKOUT }}
        ref: ${{ env.INPUT_TARGET_CHECKOUT_REF }}
        token: ${{ env.INPUT_GITHUB_TOKEN }}
        # Do not persist credentials for later steps; trunk's internal git
        # commands re-authenticate on demand via git_github.sh.
        persist-credentials: false
    - name: Locate trunk
      shell: bash
      run: ${GITHUB_ACTION_PATH}/setup/locate_trunk.sh
    - name: Determine check mode
      shell: bash
      run: |
        # Determine check mode
        ${GITHUB_ACTION_PATH}/determine_check_mode.sh
    - name: Init on-demand
      shell: bash
      run: |
        if [[ ! -e .trunk/trunk.yaml ]]; then
          ${TRUNK_PATH:-trunk} --ci init
          echo "INITIALIZED_TRUNK=true" >>$GITHUB_ENV
        fi
    - name: Detect setup strategy
      shell: bash
      env:
        ACTION_PATH: ${{ github.action_path }}
      run: |
        if [[ -e .trunk/setup-ci ]]; then
          echo "INPUT_SETUP_DEPS=true" >>$GITHUB_ENV
        else
          mkdir -p .trunk
          ln -s "$ACTION_PATH/setup-env" .trunk/setup-ci
          echo .trunk/setup-ci >>.git/info/exclude
        fi
    - name: Set up env
      uses: ./.trunk/setup-ci
      if: env.INPUT_SETUP_DEPS == 'true'
      with:
        cache-key: ${{ env.INPUT_SETUP_CACHE_KEY }}
    - name: Post-init steps
      if: inputs.post-init
      shell: bash
      env:
        POST_INIT: ${{ inputs.post-init }}
      run: |
        _post_init_script="$(mktemp)"
        printf '%s\n' "$POST_INIT" > "$_post_init_script"
        bash --noprofile --norc -eo pipefail "$_post_init_script"
        rm -f "$_post_init_script"
    - name: Cache Linters/Formatters
      if: env.TRUNK_CHECK_MODE != 'none' && env.INPUT_CACHE == 'true'
      uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
      with:
        path: ${{ env.INPUT_CACHE_PATH }}
        key: ${{ env.INPUT_CACHE_KEY }}
    - name: Run trunk check on pull request
      if: env.TRUNK_CHECK_MODE == 'pull_request'
      shell: bash
      env:
        INPUT_SAVE_ANNOTATIONS: ${{ inputs.save-annotations }}
      run: ${GITHUB_ACTION_PATH}/run_with_timeout.sh pull_request.sh
    - name: Run trunk check on push
      if: env.TRUNK_CHECK_MODE == 'push'
      shell: bash
      env:
        GITHUB_EVENT_AFTER: ${{ env.GITHUB_EVENT_AFTER || github.event.after }}
        GITHUB_EVENT_BEFORE: ${{ env.GITHUB_EVENT_BEFORE || github.event.before }}
      run: ${GITHUB_ACTION_PATH}/run_with_timeout.sh push.sh
    - name: Run trunk check on all
      if: env.TRUNK_CHECK_MODE == 'all'
      shell: bash
      run: ${GITHUB_ACTION_PATH}/run_with_timeout.sh all.sh
    - name: Run trunk check on Trunk Merge
      if: env.TRUNK_CHECK_MODE == 'trunk_merge'
      shell: bash
      run: ${GITHUB_ACTION_PATH}/run_with_timeout.sh trunk_merge.sh
    - name: Cat Trunk Cli / Daemon logs
      if: always() && inputs.cat-trunk-debug-logs == 'true'
      shell: bash
      run: |
        echo "::group::.trunk/logs/cli.log"
        if [[ -f .trunk/logs/cli.log ]]; then
          cat .trunk/logs/cli.log
        else
          echo ".trunk/logs/cli.log doesn't exist"
        fi
        echo "::endgroup::"

        echo "::group::.trunk/logs/daemon.log"
        if [[ -f .trunk/logs/daemon.log ]]; then
          cat .trunk/logs/daemon.log
        else
          echo ".trunk/logs/daemon.log doesn't exist"
        fi
        echo "::endgroup::"
    - name: Run trunk install to populate the GitHub Actions cache
      if: env.TRUNK_CHECK_MODE == 'populate_cache_only'
      shell: bash
      run: |
        # Run 'trunk install' to populate the GitHub Actions cache
        ${GITHUB_ACTION_PATH}/populate_cache_only.sh
    - name: Upload annotations artifact
      if: always() && env.TRUNK_UPLOAD_ANNOTATIONS == 'true'
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
      with:
        name: trunk-annotations
        overwrite: true
        path: ${{ env.TRUNK_TMPDIR }}/annotations.bin
    - name: Download annotations artifact
      if: inputs.post-annotations == 'true'
      uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9.0.0
      env:
        WORKFLOW_RUN_ID: ${{ github.event.workflow_run.id }}
        TRUNK_TMPDIR: ${{ env.TRUNK_TMPDIR }}
      with:
        # TODO(chris): We can't use the official download artifact action yet: https://github.com/actions/download-artifact/issues/172
        script: |
          const runId = Number(process.env.WORKFLOW_RUN_ID);
          let artifacts = await github.rest.actions.listWorkflowRunArtifacts({
              owner: context.repo.owner,
              repo: context.repo.repo,
              run_id: runId,
          });
          let matchArtifact = artifacts.data.artifacts.filter((artifact) => {
            return artifact.name == "trunk-annotations"
          })[0];
          if (matchArtifact) {
            let download = await github.rest.actions.downloadArtifact({
                owner: context.repo.owner,
                repo: context.repo.repo,
                artifact_id: matchArtifact.id,
                archive_format: 'zip',
            });
            let fs = require('fs');
            fs.writeFileSync(`${process.env.TRUNK_TMPDIR}/annotations.zip`, Buffer.from(download.data));
          }
    - name: Unpack annotations artifact
      if: inputs.post-annotations == 'true'
      shell: bash
      env:
        TRUNK_TMPDIR: ${{ env.TRUNK_TMPDIR }}
      run: |
        # Unpack annotations artifact
        cd "${TRUNK_TMPDIR}" && unzip annotations.zip
    - name: Post annotations
      if: inputs.post-annotations == 'true'
      shell: bash
      run: |
        # Post annotations
        ${GITHUB_ACTION_PATH}/annotate.sh
      env:
        GITHUB_EVENT_WORKFLOW_RUN_HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
    - name: Upload landing state
      if: env.INPUT_UPLOAD_LANDING_STATE == 'true'
      continue-on-error: true
      uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
      with:
        name: landing-state.json
        path: .trunk/landing-state.json
        overwrite: true
        if-no-files-found: warn
    - name: Cleanup temporary files
      if: always()
      shell: bash
      run: |
        # Cleanup temporary files
        ${GITHUB_ACTION_PATH}/cleanup.sh
